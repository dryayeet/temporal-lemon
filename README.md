# Lemon

CLI chat bot via OpenRouter (GPT-4o). Keeps internal state (mood, energy, engagement) and updates it after each turn.

**Setup**
- `pip install -r reqs.txt`
- `.env` with `OPENROUTER_API_KEY=your_key`

**Run**
```bash
python src/lem.py
```
Type `exit` or `quit` to stop.
